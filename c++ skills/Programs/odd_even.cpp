#include<bits/stdc++.h>
using namespace std;

int main()
{
	 int a=12;
   // int is_even=a%2==0;
   // int is_odd=1-is_even;
   // int res=is_even*100 + is_odd*7;
   // cout<<res;

   // finding even with % and /.
   //  if(a%10==0 || a%10==2 || a%10==4 ||a%10==6 || a%10==8)
   //  {
   //    cout<<"Even";
   //  }
   //  else{
   //    cout<<"ODD";
   //  }


   // Finding with /.
    double res = a/2.0;
   
     int val { 25 };
 
	if (val < 10);
		cout << 1;
	if (val < 20)
		cout << 2;
	else
		cout << 3;

    return 0;
}
    
	
